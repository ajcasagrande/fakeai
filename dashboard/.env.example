# FakeAI Dashboard Environment Configuration
# Copy this file to .env and adjust the values as needed

# ============================================================================
# Backend Configuration (Development Mode Only)
# ============================================================================
# This is the URL where your FakeAI backend server is running.
# Used by Vite dev server to proxy API requests during development.
#
# When running in production (built), the frontend is served by FastAPI
# and automatically uses the correct host/port via relative URLs.
#
# Common scenarios:
# - Default FakeAI server:        http://localhost:8000
# - Custom port (--port 9000):    http://localhost:9000
# - Remote server:                http://192.168.1.100:8000
#
VITE_BACKEND_URL=http://localhost:8000

# Alternative: VITE_API_URL (fallback for compatibility)
# VITE_API_URL=http://localhost:8000

# ============================================================================
# Optional Configuration
# ============================================================================

# API Key (if your backend requires authentication)
VITE_API_KEY=

# Dashboard Configuration
VITE_POLLING_INTERVAL=5000
VITE_BATCH_DETAIL_POLLING_INTERVAL=3000

# Feature Flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_NOTIFICATIONS=false
